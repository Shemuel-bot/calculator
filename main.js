const result = document.querySelector('#result');
const numbers = document.querySelectorAll('.num-button');
const operands = document.querySelectorAll('.operand');
const equalSign = document.querySelector('#equal-sign');

let firstNumOfOperation;
let secondNumOfOperation;
let 

Array.from(numbers).map((x) => {x.addEventListener('click', ()=>{result.textContent += x.textContent})});
Array.from(operands).map((x) => {x.addEventListener('click', ()=>{result.textContent += x.textContent})});

equalSign.addEventListener('click', ()=>{
});

function Set(num){
    if(){

    }
}

function Operation(){
    
}

